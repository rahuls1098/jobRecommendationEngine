import job_recommendation_engine_interface;

# Launch job recommendation interface by prompting login, then starting application
job_recommendation_engine_interface.login()
job_recommendation_engine_interface.main()
